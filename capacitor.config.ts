import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_capacitor_present',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
